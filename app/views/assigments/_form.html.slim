.text-center
  = form_for @assigment do |f|
    .field
      = f.hidden_field :menu_id, value: @menu    
      = f.label :pizza_id
      = f.select :pizza_id, @pizzas
    .actions 
      = link_to t('new_recipe'), new_pizza_path(menu_id: @menu), class: "btn btn-success"
      = f.submit t('add'), class: "btn btn-danger", disabled: @pizzas.empty?
