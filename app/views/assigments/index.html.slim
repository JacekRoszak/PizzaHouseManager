.text-center.mt-5
  h3.mb-4.underline
    | Edycja menu 

  table.mx-auto
    tbody
      - @assigments.each do |assigment|
        tr
          td 
            h4 = assigment.pizza.name
          td = link_to 'Usuń', assigment_path(menu_id: @menu.id, :id => assigment.id), data: { confirm: 'Are you sure?' }, method: :delete, class: "btn btn-danger"

  br

  = link_to 'Dodaj pizzę', new_assigment_path(menu_id: @menu.id), class: "btn btn-success"
  = link_to 'Powrót', root_path, class: "btn btn-danger"